# Skill Registry — tft-coach

Generated by `sdd-init` on 2026-09-07.

## Scan Result

No project-level or user-level skills were found under any of the standard scan
locations (`~/.claude/skills/`, `~/.cursor/skills/`, `{project}/.claude/skills/`,
`{project}/skills/`, etc.). This is expected: `tft-coach` is a brand-new, empty
project with no `.claude/skills/` directory of its own yet, and no matching
global skill directories exist on this machine outside the SDD tooling itself
(`sdd-*` and `_shared` are excluded from the registry by convention).

No project convention files (`AGENTS.md`, `CLAUDE.md`, `.cursorrules`,
`GEMINI.md`, `copilot-instructions.md`) exist yet either.

## Skills Index

| Name | Trigger | Path | Scope |
|------|---------|------|-------|
| (none found) | — | — | — |

## Re-scan

Re-run `sdd-init` (or a future phase's skill-loading fallback) once the stack
is chosen and project-specific skills/conventions are added, so this registry
picks them up.
